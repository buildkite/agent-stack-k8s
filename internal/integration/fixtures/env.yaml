steps:
  - label: ":earth_asia:"
    agents:
      queue: {{.queue}}
    plugins:
      - kubernetes:
          podSpec:
            containers:
              - image: alpine:latest
                command:
                - echo
                args:
                - >-
                  Testing some env variables:
                  $${BUILDKITE_AGENT_TOKEN+set}
