steps:
  - label: ":wave:"
    agents:
      queue: "{{.queue}}"
    image: curlimages/curl:latest
    command: curl localhost:80
    plugins:
      - kubernetes:
          sidecars:
            - image: nginx:latest
