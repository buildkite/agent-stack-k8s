steps:
  - label: ":x:"
    agents:
      queue: {{.queue}}
    plugins:
      - kubernetes:
          podSpec:
            containers:
              - image: buildkite/non-existant-image:latest
                command:
                  - "true"
