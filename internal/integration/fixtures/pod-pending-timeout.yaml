steps:
  - label: ":x:"
    agents:
      queue: "{{.queue}}"
    plugins:
      - kubernetes:
          podSpec:
            nodeSelector:
              integration.buildkite.com/non-existent-node-label: "true"
            containers:
              - image: alpine:3.17.2
                command:
                  - "true"
