steps:
  - label: ":wave:"
    env:
      BUILDKITE_SHELL: /bin/sh -e -c
    plugins:
      - kubernetes:
          containers:
            - image: alpine:latest
              command: [echo]
              args: [hello, world]
